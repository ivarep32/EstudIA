name: gestion_academica_app
description: Demo de aplicación de gestión académica en Flutter
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.10.0
  firebase_auth: ^4.4.0
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
